# Mining results (tracked snapshots)

Outputs of `npm run mine` / `npm run mine:concepts` over `corpus/` on the date in the filename (Azure gpt-4.1). Review → `catalogs/*.json` priors/nodes. See docs/MINING.md.
