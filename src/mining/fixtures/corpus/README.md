Tiny synthetic corpus used ONLY to test the miner's arithmetic. Term distributions here are engineered
(e.g. "credit note" in exactly 2 of 4 invoicing specs) so the tests can assert exact document frequencies.
These are not real specifications and carry no evidence about real archetypes.
